export const port = 8080;
//export const host = "localhost";
export const host = "https://soshi.app.br";
//export const base_url = `http://${host}:${port}`;
//export const base_url = `https://${host}:${port}`;
export const base_url = `${host}`;
